# ODR Tool Versions
# These versions are used for tagging GitHub releases and Docker images
# They don't have to match the source branch/tag - you can create unofficial versions
# Example: Build from 'next' branch but tag as v3.1.0 (unofficial release)
ODR_PADENC_VERSION=v3.1.0
ODR_AUDIOENC_VERSION=next
ODR_DABMUX_VERSION=v5.4.1
DABLIN_VERSION=v1.16.1

# Repository Configuration
# These specify which repositories and branches/tags to build from
# Repository URLs should be in format: https://github.com/owner/repo
# For branches/tags: specify tag or branch (e.g., "v3.1.0", "master", "next")
# If it starts with 'v' followed by a number (e.g., v3.1.0), it's treated as a tag; otherwise as a branch
#
# Examples:
# - Official release: VERSION=v3.1.0, BRANCH=v3.1.0 (builds from tag)
# - Unofficial release: VERSION=v3.1.0-custom, BRANCH=next (builds from branch, custom version)
# - Fork build: VERSION=v3.7.1-fork, BRANCH=main, REPO=https://github.com/yourfork/ODR-AudioEnc
# - Development build: VERSION=dev-$(date +%Y%m%d), BRANCH=develop (builds from branch)

# Repository URLs (without .git suffix)
ODR_PADENC_REPO=https://github.com/Opendigitalradio/ODR-PadEnc
ODR_AUDIOENC_REPO=https://github.com/Opendigitalradio/ODR-AudioEnc
ODR_DABMUX_REPO=https://github.com/Opendigitalradio/ODR-DabMux
DABLIN_REPO=https://github.com/Opendigitalradio/dablin

# Branch/Tag Configuration
ODR_PADENC_BRANCH=v3.1.0
ODR_AUDIOENC_BRANCH=next
ODR_DABMUX_BRANCH=v5.4.1
DABLIN_BRANCH=1.16.1

# Registry Configuration
REGISTRY=ghcr.io
ORGANIZATION=oszuidwest

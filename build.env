# ODR Tool Versions
# These versions are used for tagging GitHub releases and Docker images
# They don't have to match the source branch/tag - you can create unofficial versions
# Example: Build from 'next' branch but tag as v3.1.0 (unofficial release)
ODR_PADENC_VERSION=v3.1.0
ODR_AUDIOENC_VERSION=v3.6.0
ODR_DABMUX_VERSION=v5.2.0

# Build Configuration
# These specify which source code to build from (branch or tag)
# For all tools: specify tag or branch (e.g., "v3.1.0", "master", "next")
# If it starts with 'v', it's treated as a tag; otherwise as a branch
#
# Examples:
# - Official release: VERSION=v3.1.0, BRANCH=v3.1.0 (both match)
# - Unofficial release: VERSION=v3.1.0, BRANCH=next (like this current config)
# - Development build: VERSION=v3.2.0-dev, BRANCH=master
# - Testing build: VERSION=v3.1.1-rc1, BRANCH=feature/new-feature
ODR_PADENC_BRANCH=next
ODR_AUDIOENC_BRANCH=v3.6.0
ODR_DABMUX_BRANCH=v5.2.0

# Registry Configuration
REGISTRY=ghcr.io
ORGANIZATION=oszuidwest